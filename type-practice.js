"use strict";
var count = 0;
count += 1;
// count = '문자열'; 숫자로 선언했으므로 에러 발생
var str = 'hello world';
var done = true;
var numbers = [1, 2, 3];
// numbers.push('1'); 숫자 배열이므로 에러 발생
var mightBeUndefined = undefined; // 자료형이 문자열일수도 있고, undefined일수도 있음.
var nullableNumber = null; // number일수도 있고, null일수도 있음
var color = 'red';
color = 'yellow';
// color = 'green'; 지정된 값이 아니므로 에러 발생
